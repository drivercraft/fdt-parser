use alloc::vec::Vec;

use crate::NodeRef;

impl<'a> NodeRef<'a> {
    // pub fn to_bytes(&self) -> Vec<u8> {
    //     let mut fdt_data = crate::fdt::FdtData::new();
    //     fdt_data.build_from_node(self);
    //     fdt_data.to_bytes()
    // }
}
